{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Unit tests",
            "type": "node",
            "request": "launch",
            "program": "${workspaceRoot}/node_modules/.bin/gulp",
            "stopOnEntry": false,
            "args": ["unit-tests"],
            "cwd": "${workspaceRoot}",
            "protocol": "inspector",
            "env": {
                "NODE_ENV": "development"
            },
            "console": "internalConsole"
        }
    ]
}
